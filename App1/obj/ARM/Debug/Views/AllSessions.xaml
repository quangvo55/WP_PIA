<Page
    x:Class="App1.Views.AllSessions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <DataTemplate x:Key="AllSessionsTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding GameName}" Style="{ThemeResource ListViewItemTextBlockStyle}" />
                <TextBlock Grid.Column="1" Text="{Binding Stakes}" Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}" />
            </Grid>
        </DataTemplate>
    </Page.Resources>
    
    <Page.BottomAppBar>
        <CommandBar x:Name="bottomAppBar">
            <AppBarButton x:ConnectionId='1' Icon="Home" Label="Home"                         />
            <AppBarButton x:ConnectionId='2' Icon="Add" Label="Add"                        />
        </CommandBar>
    </Page.BottomAppBar>
    
    <Grid>
        <ListView x:ConnectionId='3' x:Name="SessionsListView" ItemsSource="{Binding}" ItemTemplate="{StaticResource AllSessionsTemplate}" IsItemClickEnabled="True"                           />
    </Grid>
</Page>

